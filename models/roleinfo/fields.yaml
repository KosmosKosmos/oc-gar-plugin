# ===================================
#  Form Field Definitions
# ===================================

fields:
    id:
        label: ID
        disabled: true
        hidden: true
    role:
        label: Role
        type: relation
        nameFrom: name
    company:
        label: Company
    street:
        label: Street
    zip:
        label: ZIP
    city:
        label: City
    confirm_by_role:
        label: Confirm by Role
        type: switch
        default: true
        comment: 'If on, only first user of the role will have to confirm the GAR'